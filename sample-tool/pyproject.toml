[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-tool"
version = "0.1.0"
description = "Small Python tool project"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
sample_tool = "sample_tool.main:main"
